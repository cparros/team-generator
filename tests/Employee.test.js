const { it } = require('@jest/globals')
const Employee = require('../lib/Employee')

describe('getName', () => {
  it('return the name of the employee', () => {
    expect(this.name)
  })
})

describe('getId', () => {
  it('return the name of the employee', () => {
    expect(this.id)
  })
})

describe('getEmail', () => {
  it('return the name of the employee', () => {
    expect(this.email)
  })
})

 describe('getRole', () => {
  it('return the name of the employee', () => {
    expect(this.role)
  })
 })